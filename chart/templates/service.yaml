apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-thread-proxy.fullname" . }}
  labels:
    {{- include "alert-thread-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "alert-thread-proxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
